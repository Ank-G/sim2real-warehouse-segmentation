experiment:
  name: model_b_randomized_v2

model:
  architecture: yolo26n-seg.pt

data:
  dataset: configs/datasets/synthetic_randomized_v2.yaml

training:
  epochs: 100
  imgsz: 640
  batch: 16
  patience: 20
  workers: 8
  seed: 42
  pretrained: true
  optimizer: auto
  cache: false
  amp: true

output:
  project: results
  name: model_b_randomized_v2
